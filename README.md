# Machine Learning Lab

## Dependencies

To install the required dependencies for this lab, please use the `requirements.txt` file provided in the repository. This file includes all necessary packages for the project, including:

- `mlxtend`
- `tensorflow`

You can install the dependencies by running the following command:

```bash
pip install -r requirements.txt
```

Make sure that you have Python and pip installed on your system.